// import * as actionTypes from '../actions';

// const initialState = {
//   articleSelected: {},
//   showUpdate: false
// };

// export const singleArticleReducer = (state = initialState, action) => {
//   switch (action.type) {
//     case actionTypes.SINGLE_ARTICLE:
//       return { ...state, articleSelected: action.payload, showUpdate: false };
//     case actionTypes.TOGGLE_UPDATE_ARTICLE:
//       return { ...state, showUpdate: !state.showUpdate };
//     default:
//       return state;
//   }
// };